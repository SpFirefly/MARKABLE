<!--
  Copyright (c) 2026 SpFirefly
  Licensed under the MIT License.
  本项目由SpFirefly开发，保留所有著作权。
-->
    <Window x:Class="MARKABLE.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MARKABLE"
        mc:Ignorable="d"
        Title="MARKABLE" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        MinHeight="500" MinWidth="700">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="1" x:Name="OpenFolderBtn" Click="OpenFolder_Click" Style="{StaticResource NavButton}" Margin="12,8,12,4" Content="打开文件夹"/>

                <Button Grid.Row="2" x:Name="FilterBtn" Click="FilterExtension_Click" Style="{StaticResource NavButton}" Margin="12,4,12,8" Content="筛选格式"/>

                <Separator Grid.Row="3" Margin="16,16,16,16" Background="{StaticResource DividerBrush}"/>

                <ListBox Grid.Row="4" x:Name="PhotoListBox" 
                         SelectionChanged="PhotoListBox_SelectionChanged"
                         Background="Transparent" BorderThickness="0"
                         VirtualizingPanel.ScrollUnit="Pixel"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         ItemContainerStyle="{StaticResource ModernListBoxItem}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="8,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" Width="32" Height="32" Margin="0,0,8,0" Stretch="Uniform"
                                       Source="{Binding Thumbnail, IsAsync=True, FallbackValue={x:Null}}"/>
                                <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" FontSize="13" Foreground="{StaticResource PrimaryTextBrush}"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Button Grid.Row="5" x:Name="SupportBtn" Click="SupportAuthor_Click" Style="{StaticResource NavButton}" Margin="84,10,84,10"
                        Background="{StaticResource AccentBrush}" Foreground="White" Content="支持作者"/>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="{StaticResource MainBackgroundBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Margin="24,24,24,12" CornerRadius="12" Background="{StaticResource CardBackgroundBrush}">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <Image x:Name="PreviewImage" Stretch="Uniform" />
                    </ScrollViewer>
                </Border>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12">
                    <TextBlock Text="评分：" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" FontSize="14" Margin="0,0,16,0"/>

                    <Button x:Name="Star1" Click="Star_Click" Tag="1" Style="{StaticResource StarButton}" Content="★"/>
                    <Button x:Name="Star2" Click="Star_Click" Tag="2" Style="{StaticResource StarButton}" Content="★"/>
                    <Button x:Name="Star3" Click="Star_Click" Tag="3" Style="{StaticResource StarButton}" Content="★"/>
                    <Button x:Name="Star4" Click="Star_Click" Tag="4" Style="{StaticResource StarButton}" Content="★"/>
                    <Button x:Name="Star5" Click="Star_Click" Tag="5" Style="{StaticResource StarButton}" Content="★"/>

                    <Button x:Name="ClearRatingBtn" Click="ClearRating_Click" Style="{StaticResource TextButton}" Margin="24,0,0,0" Content="清除"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8">
                    <Button x:Name="PrevBtn" Click="PrevImage_Click" Style="{StaticResource ActionButton}" Width="100" Margin="8" Content="◀ 前一张 Pre"/>
                    <Button x:Name="NextBtn" Click="NextImage_Click" Style="{StaticResource ActionButton}" Width="100" Margin="8" Content="Next 后一张 ▶"/>
                </StackPanel>

                <TextBlock Grid.Row="3" x:Name="FileInfoText" HorizontalAlignment="Center" Margin="0,12,0,20" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}"><Run Language="zh-cn" Text="未"/><Run Text="选择图片"/></TextBlock>
            </Grid>
        </Border>
    </Grid>
</Window>